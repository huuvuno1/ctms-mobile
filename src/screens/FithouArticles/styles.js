import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  wrapper: {
    margin: 10,
    marginBottom: 30,
  },
  article: {
    padding: 10,
    borderColor: 'green',
    borderWidth: 1,
    marginBottom: 10,
    borderRadius: 5,
  },
  title: {
    fontSize: 18,
    marginBottom: 10,
  },
});

export default styles;
